## Prompt de Execução — Uma Frase

> **"Implemente o VisiteCRM como um SaaS multi-tenant completo em monorepo pnpm/TypeScript com stack padronizada (Express + Drizzle + Clerk + React/Vite + BullMQ/Redis + PostgreSQL com RLS), seguindo esta ordem: (1) limpar duplicação arquitetural removendo Prisma/NextAuth, (2) implementar multi-tenancy com 3 camadas de proteção (middleware → Drizzle wrapper → RLS), (3) construir o fluxo completo de reserva com service layer único que atende todas as origens (agência, vendedor, loja, importação) executando em transação atômica (upsert cliente → reservar assentos com lock otimista → criar reserva → mover pipeline → criar cobrança → audit log → emitir evento), (4) implementar sincronização global via EventBus + SSE + React Query para que toda mutação reflita instantaneamente em todas as telas (dashboard, mapa de assentos, pipeline kanban, perfil do cliente, analytics), (5) integrar pagamentos via adapter pattern (Stripe/Asaas/MercadoPago) com webhooks que confirmam reserva + assento + pipeline + stats do cliente automaticamente, (6) adicionar filas BullMQ para e-mails transacionais, PDFs, webhooks e automações (Se X → fazer Y), (7) construir onboarding wizard, loja pública com auto-reserva, pipeline kanban, NPS, marketing, analytics com KPIs obrigatórios, e painel SuperAdmin com métricas SaaS (MRR/Churn/LTV/CAC), tudo organizado na estrutura de monorepo definida (packages/db, packages/shared, packages/email-templates, apps/api, apps/web, apps/store, apps/admin) com RBAC hierárquico, audit logs automáticos, rate limiting, validação Zod compartilhada e OpenAPI + Orval para auto-geração de hooks no frontend."**

---

### 💡 Versão Curta (se precisar de algo mais direto):

> **"Implemente o VisiteCRM: limpe a arquitetura duplicada padronizando em Express/Drizzle/Clerk, implemente multi-tenancy com RLS, construa o fluxo de reserva unificado (qualquer origem → mesmo service → transação atômica → evento → sincronização em tempo real via SSE em todas as telas), integre pagamentos com adapter pattern + webhooks, adicione filas BullMQ para e-mails/PDFs/automações, e entregue o sistema completo (onboarding, loja, pipeline, NPS, marketing, analytics, SuperAdmin) na estrutura de monorepo definida."**